# roles/docker/templates/docker-compose.yml.j2
version: "3.8"
services:
  client:
    image: "{{ client_image }}"
    ports:
      - 3000:3000 
    depends_on:
      - backend  
  backend:
    image: "{{ backend_image }}"
    ports:
      - 5000:5000